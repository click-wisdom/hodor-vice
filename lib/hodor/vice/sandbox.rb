require "key_space"

module Hodor
  class Sandbox

    class << self
      def expand(workspace, requester)
        # Expand an IAM Permissions Strata using the workspace passed in
      end
    end

  end
end
